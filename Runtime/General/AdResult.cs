namespace AdsManagement
{
    /// <summary>Represents the result of an ad show operation.</summary>
    public enum AdResult
    {
        Success,
        Failed,
        Interrupted,
        Timeout,
    }
}
